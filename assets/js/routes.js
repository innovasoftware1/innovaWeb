// Base URL de tu proyecto
const BASE_URL = "file:///C:/Proyectos%20INNOVA/innovaWeb/";

// Definición de rutas
const ROUTES = {
  INDEX: `${BASE_URL}index.html`,
  PRODUCTOS: {
    VALLAS: {
      METALICAS_TUBULARES: `${BASE_URL}pageProduct/tubulares.html`,
    },
  },
  CONTACTO: `${BASE_URL}contacto.html`,
};

// Exportar las rutas
export default ROUTES;
